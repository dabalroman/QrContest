enum Routes {
    login = '/',
    register = '/register',
    dashboard = '/dashboard',
    code = '/code/:code',
    user = '/user/:userId',
    help = '/help',
    rulebook = '/rulebook'
}

export default Routes;
